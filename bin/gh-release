#!/usr/bin/env bash
set -euo pipefail

DATE="$(date +"%Y-%m-%d")"
RELEASE="${1:-$DATE}"
if [[ -z "$RELEASE" ]]; then
  echo "usage: $0 <tag>"
  exit 1
fi

tar -cJf plots.tar.xz data/plots/
trap 'rm plots.tar.xz' QUIT
gh release create --fail-on-no-commits --generate-notes "${RELEASE}" plots.tar.xz
